#include "behavioral/iterator/iterator.h"

#include <iostream>

// TODO: 实现 Iterator 后，在这里写客户端用法。
//
// 典型客户端：
//   1. 不暴露集合内部（数组 / 树 / 哈希）
//   2. 通过 begin / end 或自定义 Iterator 遍历
//   3. 同一套循环可作用在不同底层结构上

int main() {
  using namespace design_pattern::behavioral::iterator;

  // for (auto it = collection.begin(); it != collection.end(); ++it) {
  //   use(*it);
  // }

  std::cout << "[Iterator] 客户端骨架：实现模式后在此补充真实用法。\n";
  return 0;
}
